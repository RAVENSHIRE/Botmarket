# BOTMARKET backend dependencies (Python 3.12)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
python-dotenv==1.0.1

# PostgreSQL driver (used by docker-compose / production).
# SQLite remains the zero-setup default for local dev.
psycopg[binary]==3.2.3

# Testing
pytest==8.3.4
httpx==0.28.1
